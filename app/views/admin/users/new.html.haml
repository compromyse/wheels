- content_for :title, "Add User"
%h1.text-3xl.font-bold.text-gray-900.mb-8 Add User

= render "form", user: @user, productions: @productions, distributions: @distributions,
    user_productions_map: {}, user_distributions_map: {}
